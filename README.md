<div align="center">

<img src="landing/banner.png" alt="Ocul-PM — 에이전트는 코드를 쓰고, 당신은 기억을 갖습니다" width="100%" />

<p><b>AI 코딩 에이전트가 코드를 쓰는 동안, 그 기록은 Ocul-PM 이 남깁니다.</b><br/>
Claude Code · Codex · Cursor · Gemini CLI 와 함께 쓰는 로컬-우선 프로젝트 매니저</p>

[![CI](https://github.com/bunhine0452/Ocul-PM/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/bunhine0452/Ocul-PM/actions/workflows/ci.yml)
[![Latest release](https://badgen.net/github/tag/bunhine0452/Ocul-PM?icon=github&label=download&color=12a06b)](https://github.com/bunhine0452/Ocul-PM/releases/latest)
[![Downloads](https://img.shields.io/github/downloads/bunhine0452/Ocul-PM/total?color=12a06b&label=downloads&cacheSeconds=3600)](https://github.com/bunhine0452/Ocul-PM/releases)
[![Platform](https://img.shields.io/badge/macOS-Apple%20Silicon-111?logo=apple)](https://github.com/bunhine0452/Ocul-PM/releases/latest)
[![Built with Tauri 2](https://img.shields.io/badge/Tauri-2-24C8A0?logo=tauri&logoColor=white)](https://tauri.app)
[![License: MIT](https://img.shields.io/badge/license-MIT-blue)](LICENSE)
[![Ko-fi](https://img.shields.io/badge/Ko--fi-%ED%9B%84%EC%9B%90%ED%95%98%EA%B8%B0-FF5E5B?logo=kofi&logoColor=white)](https://ko-fi.com/beachcombers)

<a href="https://www.producthunt.com/products/ocul-pm?embed=true&amp;utm_source=badge-featured&amp;utm_medium=badge&amp;utm_campaign=badge-ocul-pm" target="_blank"><picture><source media="(prefers-color-scheme: dark)" srcset="https://api.producthunt.com/widgets/embed-image/v1/featured.svg?post_id=1237239&amp;theme=dark"><img src="https://api.producthunt.com/widgets/embed-image/v1/featured.svg?post_id=1237239&amp;theme=light" alt="Ocul-PM — Product Hunt 에서 보기" width="250" height="54" /></picture></a>

[oculpm.com](https://oculpm.com) · [키노트](https://oculpm.com/keynote) · [위키](https://oculpm.com/wiki) · [다운로드](https://github.com/bunhine0452/Ocul-PM/releases/latest) · [변경 이력](CHANGELOG.md) · [이슈](https://github.com/bunhine0452/Ocul-PM/issues)

한국어 · [English](README.en.md)

</div>

---

에이전트한테 일을 시키는 날이 늘수록 이상한 비용이 하나 생깁니다. 지난주에 Claude Code 가 어떤 파일을 왜 건드렸는지, Cursor 가 고쳤다는 버그가 진짜 고쳐졌는지를 매번 git log 와 기억에 의존해 다시 캐내는 일입니다. 코드는 남는데 맥락은 남지 않기 때문입니다.

Ocul-PM 은 프로젝트 폴더에 규칙 파일(`AGENTS.md`) 하나를 심는 것으로 시작합니다. 에이전트는 작업 하나를 끝낼 때마다 이 규칙대로 `.oculpm/journal/` 에 마크다운 일지를 남기고, 앱은 그것을 읽어 타임라인과 일일 브리프, 변경 diff, 회고로 보여줍니다. 원본이 전부 마크다운 파일이라 코드와 함께 커밋할 수 있고, 앱이 없어도 그냥 읽힙니다.

서버는 없습니다. 데이터는 프로젝트의 `.oculpm/` 폴더와 로컬 SQLite 캐시에만 있고, 밖으로 나가는 것은 직접 부른 LLM API 호출과 새 버전 확인이 전부입니다.


<img src="landing/shots/08-receipt.jpg" alt="Ocul-PM — 앱 안의 Claude Code 가 편집 diff 와 턴 영수증을 남긴 실제 화면" />
<p align="center"><i>실제 화면 — 앱 안의 Claude Code 가 파일을 고치고, diff 로 보여 주고, 스스로 작업 일지를 남긴 턴입니다.</i></p>

## 세 가지처럼 보이지만, 하나의 앱입니다

### 📓 기록장 — 기록은 공짜여야 합니다

에이전트가 일을 마치는 순간 일지는 이미 쓰여 있습니다. 버그·기능·리팩토링으로 분류되고, 어느 에이전트가 어느 모델로 했는지가 붙습니다. 아침의 Today 브리프가 어제를 정리하고, 스탠드업·PR 본문·주간 보고·회고가 버튼 하나로 나옵니다 — 백미러가 핸들이 됩니다.

<img src="landing/shots/02-journal.jpg" alt="자동 작업 일지 — 에이전트·모델별로 분류된 일지 타임라인" />

### 🔍 검증대 — 믿지 말고 보십시오

에이전트가 만졌다는 파일을 커밋 전에 앱 안에서 줄 단위 diff 로 확인합니다. 일지와 나란히 — "말한 것"과 "실제로 바뀐 것"을 붙여 놓고 봅니다. 코드 맵은 "이 파일을 바꾸면 N개 파일에 영향"을 고치기 전에 알려 줍니다.

<img src="landing/shots/03-diff.jpg" alt="변경 diff — 에이전트가 만든 변경의 줄 단위 로컬 diff" />

### 🖥️ 콘솔 — 에이전트를 안으로

진짜 `claude` 가 앱 안에서 구동됩니다 (Agent Client Protocol). 도구 호출이 카드로 흐르고, 편집 diff 가 카드에 그대로 그려지고, 승인 카드에는 실행될 명령과 바뀔 내용이 실립니다 — 제목만 보고 허용을 누르지 않습니다. 턴이 끝나면 "도구 4 · 2분 14초" 영수증이 남습니다.

<img src="landing/shots/s2.jpg" alt="승인 카드 — 바뀔 내용의 diff 가 카드 안에 보이는 모습" />

<table><tr>
<td width="50%"><img src="landing/shots/04-graph.jpg" alt="코드 맵 — 의존성 그래프와 변경 영향 분석" /><p align="center"><i>코드 맵 — 의존이 보이면 두려움이 줄어듭니다</i></p></td>
<td width="50%"><img src="landing/shots/05-terminal.jpg" alt="⌘J 터미널 도크" /><p align="center"><i>⌘J — 어느 화면에서든 터미널</i></p></td>
</tr></table>

## 🚀 v2.33.0 — 화면에서 「AI 가 만든 티」를 걷어냈습니다 · 터미널·일지·Claude Code 의 조용한 결함들

- **디자인이 제품을 말하게** — 반짝이(✨) 아이콘 29곳은 그 자리의 동작(포맷·초안·갱신·새 프로젝트·모델)을 말하는 아이콘으로, 오늘 현황의 색 상자 지표 타일은 선화 하나와 **의미 있을 때만** 색으로, AI 패널과 Claude Code 의 가운데 히어로는 **프로젝트 이름 + 시작할 질문 목록**으로 바뀌었습니다. 유리(블러) 스크림·툴바는 불투명하게, 버튼·활성 항목의 광택과 캔버스의 색 안개, 게이지 그라데이션은 단색으로. 설정·회고·다이얼로그가 쓰던 기본 둥글기·그림자·상태색 125곳을 앱 토큰에 묶어 **테마 프리셋이 그 화면들에서도 온전히** 먹습니다. 되살아나지 않도록 `pnpm lint` 가 막습니다.
- **빠른 터미널이 프로젝트를 가로지르지 않습니다** — 탭 둘에서 펼치면 같은 셸을 나눠 쓰던 것(B 에 친 명령이 A 의 디렉터리에서 돌고, 접으면 남의 셸이 죽던 것)을 프로젝트마다 자기 셸로. 끝난 셸은 먹통 대신 **«다시 시작»** 이 뜹니다. 업데이트 때 구버전 터미널 호스트가 vim·ssh·claude 를 고아로 남기던 것, 크기 변경 실패가 성공으로 둔갑해 화면이 옛 폭에 굳던 것도 함께.
- **작업 일지 검색이 본문까지 찾습니다** — 백엔드가 본문에서 찾아 준 항목을 화면이 되버리던 것, 두 주 넘게 쉰 프로젝트가 「이전 기록 더 보기」 없이 갇히던 것, 필터 중 날짜 접기가 죽어 있던 것, 출처 필터에 갇히던 것, `_`·`%` 가 와일드카드로 살아 있던 것, 열어 둔 일지가 갱신되지 않던 것 — 여섯을 고쳤습니다.
- **Claude Code 화면이 가만히 있을 때 조용합니다** — 설정 되읽기가 스스로를 다시 불러 0.8초에 **수천 번** 어댑터를 두들기던 고리를 끊었고, 탭을 옮기면 셀렉터가 **방금 떠나온 대화의 모델·권한 모드**를 가리키던 것(「Auto 라 적혀 있는데 실은 Manual」)과 뒤에서 도는 대화의 제목이 보고 있는 탭을 개명하던 것을 대화별 장부로 갈랐습니다.

## v2.32.0 — 앱 안의 Claude Code 최신화 · 계획 수락의 「대화 비우기」

- **내장 Claude Code 25 패치 상향** — 이 앱은 Claude Code 를 따로 깔라고 하지 않습니다. AI 패널이 쓰는 어댑터가 실행 파일을 함께 들고 오기 때문입니다. 그 어댑터를 올리면서 안에 든 Claude Code 도 새것이 됐습니다 — 앱을 새로 켜면 알아서 받아 두므로 **누를 버튼은 없습니다.**
- **대화 제목이 커서를 따라다니지 않습니다** — 예전에는 AI 가 제목을 붙이기 전까지 **마지막 지시문이 곧 제목**이라 대화를 이어 갈수록 탭 이름이 계속 바뀌었습니다. 이제 대화 내용을 보고 한 번 지어 붙이고 그대로 둡니다.
- **노력 수준이 모델마다 기억됩니다** — 모델을 바꿔도 골라 둔 값이 초기화되지 않습니다.
- **계획 수락의 「대화 비우기」가 눈에 띕니다** — 계획 모드를 마칠 때 계획만 들고 **새 대화로 이어 가는** 선택지가 생겼습니다. 컨텍스트가 넉넉해지지만 지금까지 오간 이야기는 사라지고 되돌릴 수 없어서, 옆의 「그냥 허용」과 같은 버튼으로 두지 않았습니다 — 그 하나만 경고색이고 무엇이 사라지는지 버튼 **위에** 적힙니다.

## v2.31.0 — 링크 한 번으로 테마를 · 웹에 변경 이력과 개인정보

- **테마 갤러리와 링크 설치** — [oculpm.com/themes](https://oculpm.com/themes) 에서 라이트·다크 미리보기를 보고 「앱에서 가져오기」를 누르면 앱이 **확인 창**을 띄우고, 승인해야 실제로 받아옵니다. 가져와도 **지금 쓰는 테마는 그대로**입니다 (갤러리에 한 장 늘 뿐). 테마는 JSON 한 장이라 `landing/themes/` 에 PR 로 기여할 수 있습니다 — 저장소 테스트가 스키마·색 값·본문 대비를 검사합니다.
- **웹 변경 이력** — [oculpm.com/changelog](https://oculpm.com/changelog). 앱의 **설정 → 업데이트** 탭과 같은 내용이고, 파일 한 장에서 웹·GitHub·앱이 함께 나옵니다.
- **무엇이 나가고 무엇이 절대 안 나가는가** — [oculpm.com/privacy](https://oculpm.com/privacy). 앱이 여는 연결은 **다섯**뿐입니다 (LLM 요청 · 업데이트 확인 · GitHub 조회 · 임베딩 모델 최초 1회 · Notion 옵인). 사용 통계와 크래시 리포트는 **수집하지 않습니다**.
- **자동화 2분 가이드** — 위키 [자동화](https://oculpm.com/wiki/automation) 면 (한국어·영어).
- **플러그인 문서 → 스킬 카탈로그** — 제3자 스킬 25종에 **고정된 커밋**과 원문 링크. 「커밋 고정 사본」이라는 말을 그 자리에서 확인할 수 있습니다.

## v2.30.0 — 지난 대화를 들여옵니다 · 설정을 한 장으로 · 끊겨도 이어갑니다

- **지난 대화 임포트** — Claude 등에서 내보낸 대화 파일을 열어 이 프로젝트와 관련된 것만 골라 일지로 옮깁니다 (**설정 → 데이터**). 목록을 훑는 것까지는 **전부 오프라인·무과금**이고, 고른 대화에만 배경 모델이 붙습니다. **원본 날짜 그대로** 그 날의 폴더에 들어가고, 같은 파일을 두 번 열어도 이미 들여온 것은 「들여옴」으로 표시된 채 다시 과금되지 않습니다. 들여온 기록은 **「검토 안 함」** — 다른 곳의 대화를 모델이 옮겨 적은 것이니 읽고 확인하기 전까지는 초안입니다.
- **선언적 설정** — 규칙·스킬·자동화·앱 설정을 YAML 한 장으로 내보내 저장소에 올리고, 동료의 문서를 열면 **무엇이 달라지는지 먼저 보여 준 뒤** 적용합니다. API 키는 담기지 않습니다. 적용 뒤 다시 계산해 남은 차이가 있으면 「일부만 적용됨」이라고 말합니다. 터미널에서도 `oculpm config export|plan|apply`.
- **Claude 플러그인 번들 임포트** — 스킬·커맨드·에이전트·MCP 가 든 번들(`.zip` 또는 GitHub `owner/repo`)을 Claude Code 가 읽는 자리에 그대로 놓습니다. **손으로 고친 파일은 절대 덮어쓰지 않고** 충돌로 보고합니다. 이행하지 않는 선언은 숨기지 않고 적습니다. `oculpm://` 딥링크도 **확인 없이 실행되는 것이 없습니다**.
- **첫 실행 마법사** — 처음 켜면 언어 · 모양(밝게·어둡게·강조색) · 첫 프로젝트를 한 번 묻습니다. 이미 쓰고 있던 분에게는 뜨지 않습니다.
- **업데이트 후 창 복원** — 업데이트 재시작은 사용자가 고른 중단이 아닙니다. 재시작 직전의 창·탭 구성(떼어낸 터미널 창까지) 그대로 되살립니다.
- **오프라인에서 조용히 잘못되지 않습니다** — 폴백이 대신 답하면 **그 답변에 배지**가 붙고(그 한 번뿐, 설정은 그대로), 못 닿는 프로바이더는 **사라지지 않고 흐려지며**(이유는 툴팁에), 자동화는 실패가 아니라 **연기**되어 연결되면 따라잡습니다.
- **탭이 서로 섞이지 않습니다** — 한 창에 프로젝트를 여럿 열 수 있게 된 뒤 창 전역이던 것 아홉 곳이 프로젝트를 가로질러 샜습니다(「미기록 변경」·색인 진행률·플래너 ▶실행이 **남의 프로젝트 터미널**로 가던 것까지). 전부 탭 단위로 갈랐습니다.
- **터미널 리사이즈** — 분할 막대를 끌 때 크기 통보가 순서 없이 날아가 글자가 깨지고, claude code 출력이 두 번 찍히고, 스크롤이 어긋나던 세 증상을 한 뿌리에서 고쳤습니다.
- **영문 랜딩** — [oculpm.com/en](https://oculpm.com/en). 위키만 영어였고 정작 첫 페이지가 한국어뿐이었습니다.

## v2.29.0 — 매번 같은 것을 다시 읽지 않습니다 · 과거는 물었을 때만

- **능력 목록만 싣습니다** — 질문마다 계획 전체·최근 일지·작업 규칙이 통째로 다시 실려 가던 것을 멈췄습니다. 대화 시작 때 **무엇이 있는지 목록만** 한 번 싣고 본문은 필요할 때 꺼냅니다. 그 목록은 대화가 끝날 때까지 **한 글자도 바뀌지 않습니다** — 앞부분이 고정돼야 모델 쪽 캐시가 삽니다.
- **회상 신호가 있을 때만 과거를 붙입니다** — 「지난주에 뭐 했지」·「내가 뭐라고 했지」·「계획 어디까지 했지」 같은 질문에만 일지·계획이 붙습니다. 판정은 한/영 신호어 사전이고 **AI 를 부르지 않습니다**. 상한(약 800토큰)을 넘으면 관련도 낮은 것부터 **통째로** 뺍니다 — 반쪽 기록은 넣지 않습니다.
- **규칙을 자르지 않습니다** — 예전엔 규칙을 2,500자로 잘라 매번 넣었고, 그 절단이 한 번은 **「시크릿 금지」 조항을 통째로 삼켰습니다**. 이제 절대 잊으면 안 되는 **세 줄**만 항상 남고 나머지는 필요할 때 **전문**이 옵니다. 의심스러우면 `/rules` — `/plan`·`/journal`·`/skill` 도 그 자리에서 전문을 밀어 넣습니다.
- **관련도 감쇠** — 실제로 실린 기록은 점수가 오르고, 오래 안 쓰이면 30일 반감기로 흐려집니다. 예산이 모자랄 때의 순서를 이게 정합니다. 통계일 뿐이라 지워도 일지·계획·규칙 파일은 그대로입니다.
- **설정 → 컨텍스트** — 항상 따라가는 지시(전역/프로젝트별) · 이번 대화가 받는 목록 원문 · 마지막 회상 예산 · 관련도 순 후보(개별 「잊기」) · 통계 초기화. 프로젝트 지시가 전역보다 우선합니다.
- **스킬 키워드** — 스킬에 검색어를 붙입니다. 검색은 이름·설명·키워드만 보고 지시문 본문은 보지 않으므로, 분류어 대신 실제로 칠 말을 적는 편이 잘 걸립니다.

## v2.28.0 — 색을 직접 만들어 파일로 주고받습니다 · 프로젝트마다 다른 색

- **테마가 파일이 됐습니다** — 여태 테마는 앱에 박힌 다섯 벌이 전부였습니다. 이제 **설정 → 모양**에서 만들고, `.json` 으로 내보내고 가져옵니다. 내장 다섯 벌도 같은 형식이라 「복제해서 편집」으로 바로 출발합니다 — 내장이 곧 예제입니다.
- **앱 자체가 미리보기** — 별도의 미리보기 상자가 없습니다. 색을 하나 바꾸면 그 순간 사이드바·카드·경계·상태색이 전부 바뀝니다. **바꾸지 않은 색은 건드리지 않습니다** — 배경만 다섯 줄 적은 테마도 온전한 테마이고 나머지는 밝게/어둡게 기본값을 물려받습니다. 토큰마다 「가족 기본값으로 되돌리기」.
- **강조색이 조용히 사라지지 않습니다** — 강조 계열을 하나도 지정하지 않은 테마는 고른 강조색을 그대로 둡니다. macOS 라면 「시스템 강조색 따르기」로 OS 설정 색을 그대로 입힐 수 있습니다.
- **프로젝트마다 다른 테마** — 프로젝트 편집에서 그 프로젝트의 테마를 정하면 **그 프로젝트를 보고 있는 창만** 그 색으로 그려집니다. 정하지 않으면 전역 설정을 따릅니다. 사이드바의 프로젝트 색 마크는 그대로 — 하나는 표면 전체, 하나는 표식입니다.
- **남의 테마가 내 것을 덮지 않습니다** — 가져온 파일은 원래 식별자를 버리고 새로 발급받습니다(같은 파일을 두 번 가져오면 서로 다른 두 테마). 이름이 겹치면 **묻습니다** — 덮어쓸지, 사본으로 둘지. 파일은 한 번만 고르면 됩니다. 테마가 칠할 수 있는 것은 정해진 색 목록뿐이라 파일에 다른 무엇이 적혀 있어도 화면에 새지 않습니다.

## v2.27.0 — 손이 멎으면 알아서 기록합니다 · 누가 시켰는지 보입니다

- **감시 자동화** — 지난 버전의 자동화는 시계만 봤습니다. 이제 **작업 폴더를 감시**해 파일 변경이 멎고 정해진 시간이 지나면 일지 초안을 남기거나 계획을 갱신합니다. 기다릴 시간은 여섯 단계 — 빠름(0.2초)·균형(1초)·느긋(3초)·여유(1분)·지연(5분)·확장(10분).
- **Claude Code 를 안 쓰는 작업도 남습니다** — 자동 일지 초안은 여태 Claude Code 종료 훅에서만 생겼습니다(Cursor·에디터 직접 편집은 흔적 없음). 감시가 **두 번째 경로**가 되어 메웁니다. 두 경로가 같은 구간을 두 번 적지 않게 자리를 예약하고, 진 쪽은 **어느 경로가 이겼는지**를 실행 기록에 남깁니다.
- **자기 꼬리를 물지 않습니다** — 감시가 일지를 쓰고 그 쓰기를 다시 보는 증폭 루프를 구조로 막습니다. 일지·플래너·자동화·색인 쓰기는 **발동 원인에서 제외**(화면 갱신은 그대로), 같은 자동화는 반응성 단계의 두 배 안에 재발동 없음, 하루 상한이 마지막 그물.
- **출처 배지** — 기록이 여섯 곳에서 오는데(직접·에이전트·MCP·자동 초안·스케줄·감시) 전부 같은 카드였습니다. 이제 일지 카드·상세·오늘 피드·회고에 배지가 붙고, 일지 화면에 **출처 필터 줄**이 섭니다. 출처가 한 종류뿐이면 그 줄은 그리지 않습니다.
- **열지 않고 중단** — 대화 목록의 각 줄이 **「실행 중…」·「입력을 기다립니다」**를 말하고, 도는 대화가 맨 위로 오며, 줄 위 버튼으로 그 자리에서 멈춥니다. 설정 → 자동화 카드에도 같은 「중단」.
- **닥터 자동화 5줄** — 배경 모델(없으면 자동화 전부 정지 + 고치러 가는 문) · 활성 스케줄과 다음 시각 · 활성 감시와 반응성 · **오늘 실행 / 하루 상한** · 최근 실패와 사유. 켜 두었는데 설정이 어긋나 조용히 안 도는 정의도 따로 셉니다.
- **한 번도 안 걸린 규칙** — 진단에 **발동** 구역. 최근 7일 실제 발동을 많은 순으로, 그 아래 **한 번도 안 걸린 규칙**. 아래쪽이 진짜 값입니다 — 써 놓고 아무 일도 안 하는 규칙은 눈으로 안 보입니다. 안 세어 봤으면 "안 걸렸다"고 주장하지 않고, 경로 조건 때문에 안 걸리는 규칙은 그 사실을 함께 표시합니다.

## v2.26.0 — 정해진 시각에 스스로 돌아봅니다 · 스킬·규칙이 한 화면으로 · 무관한 규칙을 짚어 줍니다

- **스케줄 자동화** — 설정 → **자동화**에 "매주 금 17시에 이번 주 커밋과 미결 항목을 요약해라" 같은 지시문을 걸어 두면 앱이 그 시각에 스스로 돌립니다. 빈도 여덟 가지(한 번만·N분·N시간·매일·매주·매월·매년·cron), 빈 화면 대신 **주간 개발 요약 · 아침 브리핑 · 월간 회고** 세 예시를 꺼진 채로 내밉니다. 정의는 `.oculpm/automation/` 의 마크다운이라 손으로 고치고 팀과 커밋할 수 있습니다.
- **배경 작업 모델을 따로 고릅니다** — 자동 화해·일지 초안이 대화용 모델을 그대로 써서, 대화 모델을 바꾸면 배경 비용이 말없이 따라 올랐습니다. 이제 설정 → LLM 의 **배경 작업 모델**을 쓰고, 고르지 않으면 자동화는 오류 없이 조용히 건너뜁니다. 이미 켜 두신 분은 바뀌는 것이 없습니다 — 업데이트가 대화 모델 값을 한 번 복사하고 그 사실을 알려 드립니다.
- **안 돈 이유를 말합니다** — 실행 기록에 성공만이 아니라 **건너뜀·버림·실패가 사유와 함께** 남습니다(배경 모델 없음 · 하루 상한 소진 · 다른 자동화 실행 중). 맥이 자고 있었으면 **딱 한 번만** 따라잡고, 매달 31일 일정은 2월에 말일로 당겨 돌며, 서머타임에 사라지는 시각은 건너뛰지 않고 한 시간 밀려 돕니다.
- **스킬·규칙이 세 구역으로** — 다섯 탭(스킬·샵·규칙·훅·플러그인)을 한 화면으로 접었습니다. 위에 **컨텍스트 예산 바**, 가운데에 스킬·규칙·CLAUDE.md 를 발동 많은 순으로 세운 한 목록, 아래에 제안 인박스. 30일 0회와 꺼 둔 스킬은 접힌 **휴면**으로 스스로 내려갑니다.
- **규칙을 사건 위에서 만듭니다** — 규칙은 "에이전트가 또 같은 실수를 했을 때" 태어나는데 그 순간 사용자는 스킬 화면에 없습니다. 이제 **일지(버그·에러) · 변경 diff · 터미널 명령 블록 · Today · ⌘K** 다섯 자리에서 만들고, 폼은 그 사건에서 뽑은 값으로 미리 채워집니다.
- **이 프로젝트와 무관한 규칙** — glob 이 파일을 하나도 못 맞추는지, 규칙이 가리키는 스택이 감지된 스택에 없는지 두 신호로 지목합니다(LLM·네트워크 0). 예산 바가 그 몫을 빨간 조각으로 떼어 그리고, 누르면 **범위 좁히기 · 정리 · 스킬 트리거 교정**으로 이어집니다. 범위를 좁힐 때는 원본을 `.bak` 으로 남기고 `paths` 줄만 갈아 끼웁니다.
- **코드 트리가 스스로 최신이 됩니다** — 열어 둔 파일의 본문은 이미 밖의 변경을 따라오는데 트리만 ⟳ 를 눌러야 했습니다. 파일을 만들고 지우는 것이 대개 밖에서 도는 에이전트인 앱에서 기본값이 틀려 있었습니다.

## v2.25.0 — 떼어낸 창을 다시 붙입니다 · 처음 켠 날을 설명합니다 · 진단 닥터

- **떼어낸 창이 되돌아옵니다** — 지난 버전에서 탭을 끌면 진짜 창이 되어 손을 따라오게 됐는데, **한 번 놓은 창은 돌아올 길이 없었습니다**. 그 창의 탭을 다시 끌면 받는 쪽 탭 줄에 꽂힐 자리까지 그려 놓고 손을 놓으면 아무 일도 없었습니다 — 「탭 하나뿐인 창은 떼어낼 것이 없다」는 판정이 붙이는 길까지 막고 있었습니다. 이제 탭이 하나뿐인 창은 **창 자체가 손에 들리고**(크롬과 같습니다), 다른 창의 탭 줄에 놓으면 합쳐지며 빈 창은 닫힙니다. Escape 로 무르면 창이 제자리로.
- **처음 켠 날을 설명합니다** — 프로젝트를 추가하면 앱이 `.oculpm/` 를 만들고 `AGENTS.md` 에 기록 규칙을 넣고 `.gitignore` 를 손보는데, 그 사실을 아무도 말해 주지 않아 어느 날 `git status` 에 낯선 파일이 잔뜩 떴습니다. 이제 첫 화면이 **무엇이 만들어졌고 무엇을 커밋하면 되는지** 카드로 말합니다. 활성화 실패도 조용히 넘기지 않고 「지금 활성화」.
- **작업 중인 탭은 그냥 닫히지 않습니다** — ⌘W·탭 × 가 명령이 도는 터미널도, 대답을 만드는 에이전트 세션도 아랑곳없이 닫았습니다. 이제 그럴 때만 한 번 물어봅니다(조용한 탭은 그대로 바로). 삭제·초기화 확인 창도 앱 안에서 같은 모양으로.
- **진단 닥터** — 설정 → 진단에 아홉 줄(감시·잠금·어댑터·API 키·색인·셸 통합·훅·MCP)이 서고, 문제는 **그 자리에서** 고칩니다. 세션 기록이 어긋나면 경고 목록으로 모으고 토스트에서 바로 이동.
- **「결과 없음」과 「아직 안 만들었음」을 구분합니다** — 색인 없는 프로젝트의 검색·코드 맵이 「결과 없음」이라 해 고장처럼 보였습니다. 이제 색인이 없다고 말하고 **만들기**를 함께 줍니다. ⌘/ 로 단축키 전체를 보고, 새 버전 직후 한 번 무엇이 바뀌었는지 알려 줍니다.
- **켜자마자 뜨는 속도** — 첫 화면 코드 덩어리 538KB → 268KB(언어 사전을 필요한 것만 나중에), 하루치 요약을 날짜마다 묻던 것을 한 번에(Today 17→5, 일지 30→4), 색인 중 화면 전체 재그리기 중단, 의미 검색은 열려 있는 프로젝트 안에서만.
- **작게** — 계획 기록표에서 22줄이 조용히 사라지던 파서 결함(`|` 가 든 메모) · 감시가 도는데 늘 「감시 꺼짐」이던 상태바 · Esc 로 안 닫히던 ⌘K 팔레트 · 화면을 안 옮기던 「일지로 남기기」 · Today 가 열릴 때마다 띄우던 git 프로세스 15개 · 화면마다 제각각이던 오류 표시를 「다시 시도」가 달린 한 카드로 · 앱 전체 색·글자·그림자를 토큰 하나로 모아 라이트/다크·프리셋에서 어긋나던 자리 제거.

## v2.24.0 — 탭이 진짜 창으로 떨어져 나옵니다 · 규칙이 걸리는지 보입니다 · 색인이 열 배 가벼워졌습니다

- **탭을 끌면 진짜 창이 됩니다** — 지금까지 떼어내는 동안 손에 들린 것은 창 안에 갇힌 그림자였습니다. 커서가 창 밖으로 나가면 가장자리에 붙어 멈추고, 놓아야 창이 생겼습니다. 이제 탭이 줄을 벗어나는 **그 순간** 창이 되어 화면 밖으로도, 다른 앱 위로도 따라옵니다. 다른 창의 탭 줄에 겨누면 들고 있던 창이 감춰지고 꽂힐 자리가 보입니다(크롬과 같습니다). 세손가락 드래그가 글자를 훔치던 것도 끝.
- **규칙·스킬 발동 배지** — 정성껏 쓴 규칙이 한 번도 안 읽혔는지, 매 세션 수십 KB 를 먹는지 알 길이 없었습니다. 이제 항목마다 **「30일 N회」** 또는 **「안 걸림」**, 규칙 탭 부제에 세션당 조건부 규칙 주입량(KB). Claude Code 대화 기록을 이 컴퓨터 안에서 결정론적으로 셉니다(LLM · 네트워크 0). 「발동 다시 세기」로 처음부터, `paths` 없는 규칙은 「매 세션」.
- **코드 검색 색인 정리** — `git init` 안 한 프로젝트에서 `.gitignore` 가 무시돼 `node_modules` 가 색인되고, 한 줄짜리 압축 파일이 심볼마다 그 줄을 복제해 DB 가 558MB 까지 부풀었습니다. 벤더 폴더 차단 · 생성 파일 스킵 · 업데이트 시 소음 정리(실측 382MB). 설정 → 진단에 **파일 크기 · WAL · 빈 공간 · 큰 표** 와 「압축」. 「인덱스 재구축」이 이제 이름대로 비우고 처음부터.
- **끈 것이 진짜로 끝납니다** — 세션을 닫아도 ^D 를 무시하는 vim · ssh · 도구를 부르는 claude 는 살아남고 끝난 셸은 좀비로 쌓였습니다. 앱을 끄면 Claude Code 어댑터가 고아로 남았습니다. 이제 닫기는 포그라운드까지 내리고 회수하며, 종료는 어댑터가 내려갈 때까지 잠깐 기다립니다.
- **일지 검토 고리** — 상세에 **「검증」** 토글(카드에 초록 체크, 「검증됨」 필터가 비로소 동작). 일지 사이 **관련 링크**(막음 · 막힘 · 후속 · 중복)가 칩으로, 누르면 이동. MCP 도구로 쓰는 일지에도 관련 링크가 들어가고, 비밀이 마스킹되면 에이전트에게 알립니다.
- **작게** — 복구 불가한 옛 일지의 변경 내역을 열 때마다 git 에서 다시 찾던 것 중단 · 자동 화해 실패 알림 · 폰에서 일지 경로로 폴더 밖 파일에 닿던 틈 차단 · 하루 6MB 로그의 원인(잘리지 않던 오류 본문) 절단 · 디버그 패널의 어댑터 준비/설치 안내 · 동작 없던 「journal/ 를 git commit 으로 추적」 스위치 제거.

## v2.23.1 — 끌면 손을 따라옵니다 · 접힌 목록을 다시 그렸습니다

- **끌리는 것이 커서를 따라옵니다** — 세션을 가장자리로 끌 때도, 탭을 다른 창으로 옮길 때도 정작 **끌리는 것 자체는 제자리**에 있었습니다(흐려지거나 그림자만). 손은 움직이는데 물체는 가만히 있고 옆 것들만 툭툭 자리를 바꾸니 뻑뻑하게 끌리는 느낌이 났습니다. 이제 세션은 이름표가 손끝에 붙어 따라오고, 창 탭은 탭 자신이 크롬처럼 손을 따라 미끄러집니다.
- **자리 표시가 늦게 오지 않습니다** — 「여기에 놓입니다」를 알리는 선과 상자에 부드러운 이동이 걸려 있어, 커서는 이미 다음 칸인데 표시만 뒤에서 미끄러져 왔습니다. 따라오는 것은 손에 들린 것 하나로 충분하므로 자리 표시는 곧바로 붙습니다.
- **화면 사이 틈에 놓아도 붙습니다** — 나뉜 화면 사이와 가장자리의 얇은 여백 위에서는 미리보기가 꺼졌다 켜지고, 거기서 손을 놓으면 아무 일도 일어나지 않았습니다. 이제 가장 가까운 화면의 맞닿은 쪽으로 붙습니다(한가운데=취소는 그대로).
- **끄는 동안 무거워지지 않습니다** — 마우스가 움직일 때마다 살아 있는 터미널 화면 전부를 다시 그리고 있었습니다. 이제 한 프레임에 한 번만 계산하고, 겨누는 자리가 그대로면 아무것도 다시 그리지 않습니다.
- **접힌 세션 목록** — 상태 점과 아이콘이 나란히 끼어 좌우에 3픽셀만 남던 것을 고쳤습니다. 접힌 카드는 **아이콘 하나**로 읽히고 상태는 모서리의 작은 점으로 얹힙니다 — 평상시(성공·대기 없음·통합 꺼짐)에는 그리지 않습니다. 접힌 목록의 「+ 새 세션」 버튼이 눌려 보이던 것도 함께.

## v2.23.0 — 어느 에이전트가 기다리는지 · 명령마다 자리를 갖습니다

- **에이전트 대기 감지** — Claude Code 를 서너 개 띄우면 어느 창이 생각 중이고 어느 창이 내 대답을 기다리는지 하나씩 눌러 봐야 알았습니다. 셸이 알려 주는 건 "명령이 돌고 있다" 까지라, 한 번 뜬 에이전트는 몇 시간이고 같은 명령으로 남기 때문입니다. 이제 에이전트가 사람을 부르면(터미널 벨) 그 세션이 **노랗게** 서고 목록 위에 「N개가 기다립니다」가 뜹니다 — 누르면 그리로 가고, 여러 개면 누를 때마다 다음 것으로 돕니다.
- **추정은 추정이라고 적습니다** — 벨은 프로그램이 직접 부른 것이라 확실하지만 「20초째 조용하다」는 추정입니다(오래 생각하는 중일 수도 있습니다). 둘을 같은 표시로 그리면 기능 전체를 믿을 수 없게 되므로 문구를 갈랐습니다.
- **명령 블록** — 긴 로그를 몇 번 뱉고 나면 스크롤백은 어디가 어느 명령의 출력인지 알 수 없는 강이 됩니다. 이제 명령마다 왼쪽에 **상태 막대**가 서고(성공 초록 · 실패 빨강 · 도는 중 맥동), 오른쪽 가장자리 띠에 **실패 지점이 점으로** 찍혀 어디서 깨졌는지 스크롤바만 보고 압니다. **⌘↑ / ⌘↓** 로 명령 사이를 건너뛰고, 긴 출력을 내려보는 동안 **어느 명령의 출력인지**가 맨 위에 고정됩니다.
- **터미널에서 한 일을 그 자리에서 일지로** — 상태 막대를 누르면 명령/출력 복사 · 프롬프트에 채우기와 함께 **「일지로 남기기」·「플래너에 붙이기」**가 나옵니다. 일지는 빈 화면으로 열리지 않고 명령줄 · 종료코드 · 걸린 시간 · **출력 끝 40줄**이 채워진 채 열립니다. 플래너는 계획과 단계를 고르면 그 명령이 항목으로 들어갑니다.
- **다시 실행은 일부러 없습니다** — 스크롤백에서 고른 명령을 눈으로 확인하지 않고 바로 돌리는 것은 위험한 명령을 두 번 실행하는 가장 쉬운 길입니다. **프롬프트에 채워** 두고 Enter 는 사람이 칩니다.
- **모르는 것을 초록으로 칠하지 않습니다** — 셸 통합이 꺼져 있으면 아무 색도 입지 않고, 셸이 종료코드를 알려 주지 않은 명령은 성공이 아니라 회색입니다. 성공으로 그리면 진짜 실패를 놓칩니다.

## v2.22.0 — 터미널이 세로 세션 목록으로 · 끌어서 나란히 · 탭을 창 사이로

- **세로 세션 레일** — 가로 탭 줄을 버렸습니다. 다섯 개가 넘으면 이름이 `cla…` 로 뭉개지고 상태를 실을 자리가 없었기 때문입니다. 이제 카드 한 장에 **상태 점 · 에이전트 · 이름 · 경과 시간 · 마지막 명령**이 함께 실리고, 개수가 늘어도 아래로 흐를 뿐 좁아지지 않습니다. 머리줄 버튼으로 아이콘만 남게 접힙니다.
- **곁눈질로 읽는 상태** — 실행 중·실패한 화면은 위 가장자리에 색 띠를 두르고, 타이핑이 들어가지 않는 쪽은 가라앉습니다. 아래 줄은 왼쪽=어느 폴더 · 가운데=지금 무슨 일(1초 갱신) · 오른쪽=조작으로 다시 짰습니다. 글자 크기와 **따로** 조절하는 밀도(넉넉/표준/조밀)가 생겼습니다.
- **세션을 끌어다 나란히** — 지금까지 화면을 나누는 길은 ⌘D 뿐이라 이미 띄운 세션 둘을 같이 볼 수 없었습니다. 목록에서 세션을 집어 **화면 가장자리로 끌면 그 자리에 갈라집니다**(놓기 전에 차지할 자리가 보이고, 한가운데는 취소). 나뉜 화면의 손잡이(⠿)로는 자리를 바꾸거나 **독립 세션으로 빼냅니다**. 옮기는 동안 셸은 끊기지 않습니다.
- **탭을 창 사이로** — 떼어내기는 되는데 되돌릴 길이 없었습니다. 이제 크롬처럼 다른 창의 탭 줄에 떨어뜨리면 그 자리로 들어갑니다(꽂힐 자리 표시, 마지막 탭을 옮기면 빈 창은 닫힘). 프로젝트의 터미널·감시는 그대로 살아 있습니다.
- **끌지 않고도 — 탭 메뉴** — 탭 우클릭으로 「〜 창으로 옮기기 · 새 창으로 떼어내기 · 탭 닫기」. 키보드는 **Shift+F10**(또는 메뉴 키)로 같은 메뉴를 엽니다. 끌어다 놓기는 마우스가 있어야만 되는 조작이라 그것만으로는 절반의 사용자에게 기능이 없는 것과 같았습니다.
- **탭이 아홉 개째부터 사라지던 것** — 폭이 고르게 줄어야 하는데 어느 선에서 멈추고 그 뒤는 잘려 나갔습니다. 잘린 탭은 보이지도 눌리지도 끌리지도 않았습니다. 이제 아이콘만 남을 때까지 줄어듭니다.

## v2.21.1 — 줄번호와 코드가 겹치지 않습니다

- **가로 스크롤에서 코드가 줄번호를 뚫고 지나가던 것** — 줄번호 칸은 옆으로 밀어도 제자리에 남는데 그 칸에 바탕색이 빠져 있어, 흘러가는 코드가 숫자 밑으로 비쳐 둘이 뒤엉킨 채 읽혔습니다. 화면 밖으로 나가는 긴 줄이 있는 파일에서만 보이던 증상입니다. 이제 줄번호 칸이 편집면과 같은 바탕을 깔아 코드가 그 뒤로 지나갑니다 — 테마·색 프리셋을 바꿔도 색이 어긋나지 않습니다.

## v2.21.0 — Finder 에서 끌어다 놓기 · ⌘V 로 파일 넣기

- **Finder → 코드 트리 드래그** — 스크린샷·PDF·폴더를 프로젝트에 넣으려고 앱을 나가지 않습니다. 끌고 다니는 동안 **어느 폴더에 놓일지가 밝게 표시**되고, 놓으면 그 폴더가 펼쳐지며 어디에 들어갔는지 알려 줍니다. 파일 위에 놓으면 그 파일 **옆에** 놓입니다.
- **⌘V** — Finder 에서 ⌘C 로 복사한 파일·폴더를 앱에서 바로 붙여넣습니다. 트리에서 마지막으로 누른 폴더로 들어갑니다. 에디터 안의 ⌘V 는 종전대로 글자 붙여넣기입니다.
- **덮어쓰지 않습니다** — 같은 이름이 있으면 `note-2.txt` 로 붙습니다. 폴더는 안쪽까지, 심볼릭 링크는 제외(프로젝트 밖 내용이 사본으로 들어오는 것을 막습니다). 한 번에 2,000개·512MB 를 넘으면 거기까지만 넣고 잘렸다고 알립니다.

## v2.20.0 — 코드 화면에서 이미지·PDF 를 봅니다

- **스크린샷과 PDF 가 앱 안에서 열립니다** — 지금까지 `.png` 든 `.pdf` 든 「미리볼 수 없는 파일입니다」 한 줄이 전부라, 에이전트가 남긴 화면 캡처 하나를 보려고 Finder 로 나가야 했습니다. 이제 파일 트리에서 누르면 그림은 투명한 부분이 드러나는 체크무늬 위에 뜨고 **창 맞춤 ↔ 실제 크기**를 오갑니다(원본 해상도·용량 표기). PDF 는 문서 그대로 넘겨 봅니다. 편집기의 2MB 제한과 무관하게 **16MB 까지** — 요즘 스크린샷은 2MB 를 쉽게 넘습니다. `.svg` 는 종전대로 편집기로 열립니다(그림이자 곧 코드니까요). 열어 둔 그림을 에이전트가 갈아 끼우면 화면도 따라 바뀝니다.
- **열 수 없는 파일 위에서 누른 ⌘S 가 파일을 깨뜨리던 것** — 텍스트를 보다 이미지로 옮기면 앞 파일의 내용이 남아 있어, 저장이 그 내용을 이미지 경로로 쓰려 했습니다. 「충돌」 배너로 위장돼 덮어쓰기를 고르면 실제로 깨졌습니다.
- **활동 링이 낭독기에 무음이었습니다** — 이름표가 붙을 수 없는 자리에 붙어 무시됐고, 안쪽 그래픽은 숨김 처리, 숫자를 띄우는 말풍선은 마우스로만 떠서 낭독기에 남는 게 없었습니다. 이제 링 전체가 하나의 이미지로 이름을 갖고 "일지 N건 · 파일 N개 · +N/−N 줄"을 읽어 줍니다. 반대로 그 말풍선이 **마우스가 스칠 때마다** 소리 내어 알리던 것은 멈췄습니다.
- **링 잔손질 셋** — 프로젝트를 바꿀 때 새 기록이 생긴 것처럼 물결이 치던 것, 물결이 끝난 뒤에도 화면에 남아 있던 것, 네 자리가 넘는 수에 자릿점이 없던 것(12345 → 12,345).
- **저장소에 자동 검사가 생겼습니다** — 올라오는 모든 변경이 타입·테스트·린트·빌드를 통과해야 합니다. 앱 화면이 달라지는 변화는 아니지만, 다음 판부터 받는 빌드가 그만큼 덜 깨집니다.

## v2.19.0 — 터미널이 업데이트를 견딥니다 · 코드 화면에 전역 검색

- **앱을 업데이트해도 터미널 세션이 안 끊깁니다** — 터미널 세션을 앱과 분리된 프로세스가 쥐고 있어, 업데이트·종료 후 재실행 뒤에도 터미널 탭이 스크롤백·실행 중이던 프로그램(Claude Code 세션 포함)까지 **하던 그 자리 그대로** 되살아납니다. 터미널 탭이나 프로젝트 탭을 직접 닫을 때 세션이 정리되는 것은 종전과 같습니다.
- **프로젝트 전역 검색·치환 (⇧⌘F)** — 코드 화면 사이드바가 VS Code 식 검색 패널로 바뀝니다. 색인이 아니라 **디스크의 현재 내용**을 그대로 찾고(대소문자·단어 단위·정규식 토글), 결과를 누르면 그 매치가 선택된 채 파일이 열립니다. 치환은 한 곳·한 파일·전체(확인을 거쳐), 정규식 모드의 `$1` 그룹 참조까지 — 미저장 편집이 있는 파일은 덮어쓰는 대신 건너뛰고 알립니다.

## v2.18.0 — 폰에서도 열립니다 · 모바일 연동 (베타)

맥을 떠나도 에이전트가 뭘 했는지 볼 수 있습니다. 설정 → 모바일에서 서버를 켜고 폰 카메라로 QR 를 찍으면, **같은 Tailscale 네트워크 안의 폰 브라우저**에서 오늘 일지 · 플래너 · 논의 · AI 대화가 열립니다 — 일지를 읽고 쓰고, 플랜을 체크하고, 논의에 메모를 남기고, 맥에 설정한 AI 에게 질문하면 답이 실시간으로 흐릅니다. 홈 화면에 추가하면 앱처럼 쓰고, 데스크톱에서 고른 테마·액센트가 폰에 그대로 실립니다 (직접 만든 테마는 아직 폰에서 밝게/어둡게만 따라갑니다). **데이터는 여전히 맥을 떠나지 않습니다** — 서버는 Tailscale 사설망에만 열리고, 폰은 6자리 코드 1회 페어링 토큰으로만 접근하며, API 키는 맥 키체인에만 있습니다. 베타 기능이라 검색·회고·코드 화면은 아직 데스크톱 전용입니다.

## v2.17.0 — 실시간 갱신이 스스로 낫습니다

- **감시 자동 복구** — 에이전트가 일지·계획을 고쳐도 화면이 안 따라오고 우클릭 새로고침을 해야만 보이던 것의 원인(파일 감시가 소리 없이 죽는 두 갈래)을 막았습니다. 앱이 **1분마다 감시가 실제로 이벤트를 받는지 점검**해 먹통이면 되살리고, 시작 실패는 「복구 중」으로 알립니다. 앱을 두 개 띄우면 나중 창이 영영 읽기 전용이 되던 것도 **「가장 최근에 연 창이 주인」** 규칙으로 풀렸습니다.
- **디스패치가 화면을 빼앗지 않습니다** — 터미널이 이미 보이면(⌘J 도크·분리 창) ▶실행이 **그 자리에** 프리필하고, 대상 터미널에서 에이전트가 대화 중이면 **그 대화에 프롬프트를 붙여넣습니다**(맥락 보존). Enter 는 언제나 사용자 몫.
- **코드 탭 키보드 UX** — ⌘W 는 코드 탭 먼저(없을 때만 프로젝트 탭), ⌃Tab · ⇧⌘]·[ 탭 순환, ⇧⌘T 닫은 탭 재열기, ⌘N 새 파일. 메뉴 단축키 힌트 + 빈 화면 치트시트 확장.
- **사이드바 정비** — 문제 해결→**논의**, 변경 diff→**Diff**, 에이전트→**AI 대화**(Claude Code 와 역할 충돌 해소), **AI 섹션** 신설, 코드는 터미널 곁으로. ⌘K 에선 옛 이름으로도 찾아집니다. 설정 연동 탭은 프로젝트별/전역을 구분해 보여줍니다.

## v2.16.0 — 탭·분할·디버거, 그리고 에이전트가 바꾼 코드가 편집기 안에서 보입니다

- **탭과 좌우 분할, 파일 조작** — 여러 파일을 탭으로 열어 오가고, 화면을 좌우로 나눠 두 파일을 같이 봅니다. 트리 우클릭으로 만들기·이름 바꾸기, 드래그로 이동, 삭제는 **휴지통으로**(폴더째 잘못 지워도 되돌립니다). 이름이 바뀌거나 지워진 파일이 탭에 열려 있으면 탭과 미저장 편집이 함께 따라 움직입니다.
- **LSP 창구 완성** — ⇧F12 **참조 찾기**(원문 미리보기), 사이드바 **구조**(아웃라인), ⌘K 에 **프로젝트 전체 심볼**, 인자 입력 중 **시그니처 힌트**, ⇧⌥F **포맷**(선택 영역만도 가능, 저장 시 포맷 옵션), 줄 옆 **git 거터**. 설정 → 코드에서 언어 서버를 끄거나 경로를 지정합니다.
- **디버거 (DAP)** — 줄 번호 옆을 눌러 중단점, 멈춘 곳의 **호출 스택·변수**를 펼쳐 보고 한 줄씩 실행합니다. Rust(lldb-dap) · Python(debugpy) · Go(dlv) — 없으면 설치 방법을 알려 줍니다. [설계 근거](docs/dap/00-master-plan.md).
- **에이전트 변경 가시화** — 경로 줄의 비교 버튼으로 **마지막 커밋 이후의 변경이 본문 사이에** 나타나고(지워진 줄은 빨간 블록, 덩어리별 되돌리기), 일지 배지에서 **이 파일을 고친 작업 일지**를 골라 그 작업이 바꾼 부분만 겹쳐 봅니다.
- **얼굴 있는 파일들** — 확장자별 아이콘(TS 사각형 · 파이썬 뱀 · Rust 기어 · 리액트 원자…), 들여쓰기 안내선, 경로 브레드크럼, 무시된 파일도 트리에 흐리게.
- **문제 해결 화면 개편** — 코드 화면과 같은 편집기 + 서식 툴바, **후보 방안·다음 단계·토의 로그** 규격 블록 자동 삽입, 문서 단계에 맞는 **프롬프트 복사**(붙여넣으면 에이전트가 문서를 읽고 이어받습니다). 한글 제목 문서가 전부 같은 파일명으로 뭉쳐지던 것도 고쳤습니다.

## v2.15.0 — 「코드」 화면이 IDE 가 됐습니다

- **코드 인텔리전스 (LSP)** — 타이핑 중 **자동완성**, 틀린 곳에 **진단 밑줄**, 이름 위 **호버 타입·문서**, **F12·⌘클릭으로 정의로 이동**, 심볼 **이름 바꾸기**(쓰는 모든 파일이 함께 바뀝니다), 밑줄에 딸려 오는 **코드 액션**. Rust · TypeScript/JavaScript · Python · Go — 각 언어 서버(rust-analyzer · typescript-language-server · pyright · gopls)가 PATH 에 있으면 앱이 찾아 씁니다. **없으면 없다고 말합니다** (조용히 실패하지 않습니다). 큰 저장소의 첫 분석에 걸리는 수십 초는 상태줄이 「분석 중」으로 밝혀 고장과 구별됩니다. VS Code 포크가 아니라 표준 LSP 를 직접 붙인 것입니다 — [설계 근거](docs/lsp/00-master-plan.md).
- **코드 트리에 점 파일** — `.oculpm/` · `.github/` · `.gitignore` 가 목록에 아예 없어 정작 가장 자주 여는 것들을 코드 화면에서 못 열었습니다. 이제 보입니다 (무시된 `node_modules` 같은 것은 v2.16.0 부터 숨기는 대신 흐리게 구별합니다).
- **계획·논의 라이브 새로고침** — 터미널의 에이전트가 `.oculpm/planner/*.md` 를 고쳐도 화면은 열었을 때의 내용을 계속 보여줬습니다. 「가끔」이 아니라 늘 그랬습니다(구독이 한 줄도 없었습니다). 이제 파일이 바뀌는 즉시 따라오고, 맥이 깨거나 창으로 돌아오는 순간에도 한 번 다시 읽어 놓친 변화를 메웁니다.
- **플러그인이 과거 일지를 되찾습니다** — MCP 도구 5종 → 7종. `journal_search` 는 제목·본문·태그에 더해 **그 파일을 건드린 일지**·종류·상태·기간으로 좁히고, `journal_read` 가 고른 1건의 본문을 냅니다. 에이전트가 **작업을 시작하기 전에** 과거를 찾도록 AGENTS.md 규칙에 §0 을 넣었습니다 — 이미 내려진 결정과 이미 실패한 접근을 다시 밟지 않게.
- **사용 설명서(위키) 완성 + 영문판** — 네 쪽뿐이던 위키에 화면·개념 페이지 11종을 더했습니다. Today · 작업 일지 · 계획 · 논의 · 회고 · 에이전트 · AI 패널 · 설정 · 작업 공간 · 화면 안내 · FAQ.

## v2.14.1 — Today 가 안 열리던 핫픽스, 대화 여러 개를 나란히, 정직성 감사의 오탐 종결

- **Today 가 「오늘 데이터를 불러오지 못했어요」로 막히던 것 (v2.14.1 핫픽스)** — 붉은 상자에 `no such column: f.lines_added` 만 남고 화면이 통째로 비었습니다. v2.14.0 이 「라인 변화」를 채우려고 저장소에 더한 칸이 **앱이 예전에 써 버린 번호와 겹쳐** 일부 기기에서 만들어지지 않았습니다. 이제 앱이 켜질 때 저장소 모양을 대조해 빠진 칸을 스스로 채웁니다 — 기록은 그대로고, 앞으로 같은 어긋남이 나도 같은 방식으로 낫습니다.
- **한 프로젝트에서 Claude Code 대화 여러 개 동시에** — 탭은 예전에도 여러 개 열렸지만 한 대화가 답하는 동안 다른 대화에 친 말은 줄을 섰습니다. 보낸 말이 어디로 갈지를 「지금 보고 있는 탭」이 정했기 때문입니다. 이제 말이 향할 대화를 직접 짚습니다 — A 가 답하는 중에 새 대화를 열어 곧바로 말을 걸 수 있고, 탭을 옮겨도 답이 엉뚱한 대화로 가지 않습니다. **승인 카드도 대화마다 따로** 서서, 한 대화를 ESC 로 멈출 때 옆 대화의 승인이 함께 거절되지 않습니다.
- **정직성 감사 오탐** — 하루 60개가 「기록 안 함 · 심각」으로 떴는데 진짜는 3개였습니다. 앱이 세션에 붙이는 이름표와 에이전트가 일지에 적는 이름표가 다른 방언이라 두 집합이 영원히 안 겹쳤습니다. 판정을 **하루 단위 커버리지**로 바꿔 이름표와 무관하게 맞추고, 미리보기 앱의 임시 파일·다른 프로젝트의 기계 생성물도 걸러냅니다.
- **세션↔일지 연결** — 일지는 설명하는 작업보다 나중에 쓰이는데 세션 구간 안에 든 것만 셈해 일치·겹침이 늘 0 이었습니다. 이제 **일지가 쓰인 시각 이전에 시작한 마지막 세션**으로 귀속시킵니다(실측 겹침 0.73~0.93).
- **터미널 뷰포트** — 도크를 켜 둔 채 다른 프로젝트 탭에 갔다 오면 대화는 흐르는데 화면만 잘린 채 굳었습니다. 안 보이는 동안 터미널이 자기 높이를 0 으로 알던 것이 원인 — 다시 보이는 순간 크기를 재고 마지막 줄로 따라붙습니다.
- **변경 diff · 작업 일지의 파일 목록** — 일지가 네댓 개만 돼도 그룹이 전부 펼쳐진 채 이어 붙고 경로가 끝에서 잘려 파일명이 사라졌습니다. 그룹 접기(셋 이상이면 하나만 펼침) · 디렉터리부터 줄고 파일명은 남는 경로 · 8개부터 필터. 일지 화면의 '칩 벽'은 지금 열어 둔 파일 한 줄로 대체했습니다.
- **Today · 사이드바** — 늘 0 이라 점으로 그려지던 링의 「라인 변화」에 실제 줄 수를 싣고, 2단 배치에서 왼쪽 열 아래로 드러나던 빈 배경을 없앴습니다. 터미널을 좌·우에 붙였을 때 상단이 계단처럼 어긋나던 것, 사이드바 접힘이 프로젝트 탭마다 따로 기억되던 것도 고쳤습니다.

## v2.13.5 — 스페이스 뒤 앞 음절, 그리고 재발을 끝내기 위한 진단

- **남아 있던 두 갈래** — 「안녕」 뒤 스페이스가 「안녕 녕」이 되던 문제를 v2.13.3 에서 고쳤지만 경로가 둘 남아 있었습니다. 확정과 잔여물 사이에 **빈 입력이 한 번 끼면** 걸러낼 근거(방금 내보낸 문자열)가 지워졌고, 잔여물이 **띄어쓰기까지 끌고 오면**(「녕 」) 검사가 시작조차 되지 않았습니다. 판정을 넓힌 만큼 반대로 멀쩡한 입력을 삼키지 않는지도 함께 검사합니다.
- **정식 빌드에서도 남는 입력 기록** — 이 버그는 네 번 재발했는데 매번 트레이스 없이 고쳐야 했습니다. 기록을 켜면 앱이 느려져 문제 자체가 사라지는 종류라(타이밍 경합) 진단이 관측 대상을 바꿔 버렸기 때문입니다. 이제 입력 흐름은 **메모리 링 버퍼에만** 쌓이고(배열 한 칸 쓰기 — 타이밍을 바꾸지 않습니다), 터미널에서 **⌃⌥⇧I** 를 누르면 그 직전 흐름이 `oculpm.log` 로 저장됩니다. 앱이 잔여물로 의심되는 입력을 흘려보낸 순간에도 자동으로 남습니다.

## v2.13.4 — Claude Code 화면의 세 가지

- **「새로운 세션」 탭** — 눌러도 위쪽 탭 줄에 아무 것도 안 떠서 눌린 건지 알 수 없었습니다. 세션은 첫 마디를 보낼 때 만들어지므로 그전까지는 탭에 실릴 것이 없었기 때문입니다. 이제 점선으로 두른 임시 탭이 그 자리에 생기고, 첫 마디와 함께 같은 자리에서 진짜 탭이 됩니다(닫기·⌘W 도 됩니다).
- **제목이 마지막 지시문을 따라다니던 것** — 대화를 이어 갈수록 탭 이름이 방금 친 말로 계속 바뀌었습니다. Claude Code 가 제목을 붙이기 전까지는 마지막 문장이 임시 제목으로 쓰이기 때문입니다. 이제 그 임시 제목을 알아보고 첫 마디를 지키다가, 진짜 제목이 붙으면 그때 바꿉니다.
- **사용량 카드의 「무엇이 기여했나」** — 좁은 카드에 고정폭으로 밀어 넣어 문장이 접히고 아래가 잘렸습니다. 카드를 넓히고 비율은 막대·숫자로, 「Top skills」 같은 목록은 칩으로 정리했습니다. 항목이 늘어나도 모르는 줄은 원문 그대로 남습니다.

## v2.13.1~v2.13.3 — 터미널 한글 입력과 「코드」 화면 저장 정리

- **조합 중 스페이스·백스페이스** — 한글을 빠르게 치다 스페이스를 누르면 「안뜨」가 「안뜨뜬」이 되고, 오타를 지우면 「차」가 「ㅊ차」로 불어나던 것. 입력기의 「글자를 고쳐 쓰라」는 신호가 키 입력보다 최대 0.24초 늦게 오는 타이밍 경합과, 조합 중 백스페이스를 「입력이 끝났다」로 잘못 읽던 것이 원인이었습니다. 확정 직후 뒤늦게 도착하는 잔여 조합(「안녕 녕」)도 함께 걸러냅니다 — 개발 빌드는 느려 이 틈이 닫혀 있어 **정식 빌드에서만** 나타났습니다.
- **커서 이동 키** — 한글을 쓰다 화살표·Home/End·Esc 로 커서를 옮긴 뒤 이어 친 글자가 엉뚱한 자리를 지우던 경로를 막았습니다.
- **코드 화면 저장** — CRLF 파일이 저장 한 번에 전부 LF 로 바뀌던 것, 프로젝트 안 심볼릭 링크로 밖의 파일이 열리던 것, ⌘S 한 번에 저장이 두 번 나가 가짜 「충돌」이 뜨던 것, 미저장 편집·외부 삭제가 말없이 지나가던 것.
- **변경 diff 배지** — 내용이 `---`·`+++` 로 시작하는 줄(문서 맨 위 구분선 등)을 파일 머리글로 착각해 `+N −M` 배지가 통째로 사라지던 것.

## v2.13.0 — 새 「코드」 화면: 앱 안에서 코드를 바로 열어 보고 고칩니다

- **인앱 코드 에디터** — 파일 트리(필터 지원)에서 열면 문법 하이라이트(12개 언어)와 함께 바로 편집, ⌘S 저장. 검색 결과·코드 맵에서 「코드 화면에서 열기」로 해당 줄 점프.
- **에이전트와 안전하게 공존** — 저장 시 디스크가 그 사이 바뀌었으면 덮어쓰지 않고 배너로 선택(디스크 버전 / 내 버전). 열어 둔 파일이 밖에서 바뀌면 편집 전이면 조용히 최신화, 편집 중이면 배너.
- **미저장 편집 보존** — 파일·화면을 옮겨 다녀도 저장 안 한 편집이 남고 트리에 초록 점. 테마(라이트/다크·컬러 6종)가 에디터 색까지 물들고 ⌘F 검색도 한국어.

<details>
<summary><b>지난 버전 하이라이트</b> — v2.5 부터 v2.12.0 까지</summary>

- **v2.12.0** — 변경 diff 이미지 프리뷰(이전/현재 나란히) · 실제 파일 줄 번호 · 코드 맵 플로팅 엣지+방향 화살표 리디자인 · 코드 검색 파일별 그룹·매치 하이라이트·라인 점프
- **v2.11.0** — Claude Code 편집 diff 인라인 · 승인 카드에 명령/변경 내용 · 승인 대기 사이드바 배지 · 한글 조합 Enter 전송 버그 수정 · 대화면 마찰 일소(자동 확장·되부르기·드래그&드롭·턴 영수증·「다시 연결」)
- **v2.10.3** — 타이틀바 더블클릭은 창 크기만 · 화면 한 조각의 오류가 창 전체를 지우지 못하게 (범용 렌더 경계)
- **v2.10.2** — 터미널 도크 오른쪽 자리 · 분리 창 드래그 · macOS 창 분할 단축키(⌃⌥←→↑↓) 복구
- **v2.10.1** — ⌘J 터미널 도크 · 창으로 분리해도 셸 유지 · Claude Code 할 일 목록 · 한도 초과·모델 폴백이 대화에 기록
- **v2.10.0** — Claude Code 를 앱 안에서 직접 구동 (Agent Client Protocol)
- **v2.9** — 프로젝트를 창과 탭으로 — 크롬식 탭 · 떼어내기 · 프로젝트별 상태 기억
- **v2.8** — 화면 언어 English · 스킬 샵 (검증된 제3자 스킬 25종) · 터미널 정리
- **v2.6 – v2.7** — 시작 화면이 크로스 프로젝트 콕핏으로 · 회고 화면 (7·14·30일 신호)
- **v2.5** — Claude Code 플러그인 (아래 섹션) · 플래너 ▶실행 디스패치 · 3단계 계획 · project-inception 스킬 · AGENTS.md 토큰 60% 다이어트
- 메뉴바 상주(v2.3) · Claude 직접 연동(v2.2) — 전체 이력은 [CHANGELOG](CHANGELOG.md)

</details>

## 화면 구성

- **Today** — 오늘 무엇이 바뀌었는지 워크데이 기준으로 모아 보여줍니다. 커밋 그래프, 미커밋 변경, 에이전트가 고쳐놓고 일지에 안 적은 파일 감지(정직성 감사)까지. "스탠드업 복사"를 누르면 어제~오늘 한 일이 공유용 텍스트로 클립보드에 담깁니다.
- **작업 일지** — 에이전트가 남긴 기록의 타임라인. 어떤 에이전트가 어떤 모델로 작업했는지 표시되고, 일지마다 그 시점의 변경 diff 를 함께 보관합니다. 일지 없이 쌓여 온 저장소는 git 히스토리에서 한 번에 백필할 수 있습니다.
- **문제 해결** — 무엇을 할지 정하기 *전* 단계의 토의 문서. 문제 정의부터 후보안 비교, 결론까지 정리하고, 결론이 서면 버튼 한 번으로 플래너 계획이 됩니다.
- **Planner** — 살아있는 계획 문서. 항목마다 관련 일지가 링크되고, 원하면 새 일지가 들어올 때 계획이 따라 갱신되는 자동 화해(옵트인)도 켤 수 있습니다.
- **변경 diff** — 에이전트가 수정한 파일을 네트워크 없이 바로 비교합니다. `j`/`k` 로 파일을 오가고 `/` 로 diff 안을 검색합니다. 바뀐 파일이 코드 그래프에서 어디까지 영향을 주는지도 계산해 줍니다.
- **회고** — 최근 7·14·30일 동안 무엇을 출시했고 어디서 막혔는지, 노력이 어느 파일에 몰렸는지를 신호로 보여줍니다. AI 회고 생성, PR 본문·주간 보고 산출물, 기간 일지 `.md` 내보내기가 여기 있습니다.
- **코드 검색** — 의미(로컬 임베딩) · 심볼(AST) · 텍스트(FTS5 전문 인덱스) 세 가지 모드.
- **코드 맵** — import 만이 아니라 호출·상속·구현 관계까지 그래프로 그립니다. 파일을 고르면 "이 파일을 바꾸면 N개 파일에 영향"이 먼저 보입니다.
- **코드** — 앱 안 IDE. **탭·좌우 분할**로 여러 파일을 같이 보고, 트리에서 만들기·이름 바꾸기·드래그 이동·휴지통 삭제까지 해결하고, **Finder 에서 끌어다 놓거나 ⌘V 로** 파일·폴더를 프로젝트에 넣습니다(같은 이름은 덮어쓰지 않고 `-2` 로 붙습니다). **⇧⌘F 프로젝트 전역 검색·치환**(대소문자·단어·정규식)이 사이드바에 붙어 있습니다. **자동완성·진단·호버·정의로 이동·참조 찾기·아웃라인·이름 바꾸기·시그니처·포맷**(LSP) 에 **중단점·스텝 실행·변수 확인**(디버거, Rust·Python·Go)이 붙었고, 경로 줄의 비교 버튼으로 **에이전트가 바꾼 부분을 본문 안에서** 겹쳐 봅니다 — 이 파일을 고친 작업 일지로 바로 건너뜁니다. 에이전트가 같은 파일을 고치면 충돌 배너가 지켜 주고, 저장 안 한 편집은 화면을 옮겨도 남습니다. **이미지와 PDF 는 편집기 대신 미리보기로** 열립니다 — 그림은 창 맞춤 ↔ 실제 크기로, PDF 는 문서 그대로.
- **문서** — 프로젝트의 `docs/` 폴더를 위키처럼 탐색합니다.
- **터미널** — 앱 안 PTY 터미널. 에이전트를 여기서 돌리면 일지가 옆 화면에 쌓입니다. 세션은 왼쪽 **세로 목록**에 서고 카드마다 상태·에이전트·경과 시간·마지막 명령이 함께 보입니다. **세션을 끌어 화면 가장자리에 놓으면 그 자리에 갈라져** 둘이 나란히 서고, 나뉜 화면의 손잡이(⠿)로 자리를 바꾸거나 독립 세션으로 빼냅니다. **⌘J 로 어느 화면에서나 도크로 띄우거나(아래·왼쪽·오른쪽) 별도 창으로 떼어낼 수 있고**, 셸은 그대로 이어집니다. `/plugin`·`/mcp` 처럼 CLI 자체 대화형 화면에만 사는 기능을 쓰는 탈출구이기도 합니다.
- **Claude Code** — 진짜 `claude` 를 앱 안에서 에이전트로 구동합니다(Agent Client Protocol). 도구 호출·권한 승인·Effort/모드가 전부 대화 안 카드로 오고, 세션은 탭으로 관리됩니다. 시키는 화면입니다.
- **AI 패널** — 코드 검색·일지·플래너·git 맥락을 아는 채팅. 대화 시작 때 **능력 목록**을 한 번 싣고 본문은 필요할 때 꺼내며, 과거 기록은 회상 신호가 있는 질문에만 붙습니다(`/rules`·`/plan`·`/journal`·`/skill` 로 직접 밀어 넣을 수도 있습니다). Anthropic · OpenAI · Gemini · OpenRouter 를 지원하고, 호출이 실패하면 폴백 체인으로 다음 모델을 시도합니다. 물어보는 화면입니다.
- **스킬·규칙** — Claude Code 스킬(`.claude/skills/`)과 규칙(`.claude/rules/`, `CLAUDE.md`)을 **한 화면 세 구역**으로 관리합니다: 세션마다 들어가는 양을 말하는 **컨텍스트 예산 바**, 발동 많은 순으로 선 통합 목록(30일 0회·비활성은 접힌 「휴면」으로 자동 강등), 그리고 제안 인박스. GUI 에서 만들고 편집하고, 프로젝트 ↔ 전역(`~/.claude/skills`)으로 복사합니다. 끄면 지워지는 게 아니라 `.disabled/` 로 옮겨 로드에서만 빠집니다. 스택에 맞는 검증된 제3자 스킬 25종을 「추가하기」에서 골라 설치하고, 이 프로젝트와 **무관한 규칙**은 결정적 신호 두 가지로 짚어 범위 좁히기·정리·트리거 교정을 제안합니다. 규칙·스킬은 일지·diff·터미널 블록·Today·⌘K 에서도 바로 만듭니다.

⌘1~⌘0 으로 화면을 오가고, ⌘K 팔레트에서 일지·계획·토의·문서를 제목으로 검색해 바로 엽니다. ⌘P 는 프로젝트 전환, ⌘⇧M 은 프로젝트 관리 화면입니다. 창과 탭은 ⌘T 새 탭 · ⌘W 탭 닫기 · ⇧⌘N 새 창 · ⇧⌘W 창 닫기 · ⌃Tab · ⌘⌥←→ 로 다룹니다. 탭은 **끌어서** 순서를 바꾸고 창 밖으로 떼어내거나 다른 창의 탭 줄에 합칩니다 — **우클릭**(키보드는 Shift+F10)하면 같은 일을 메뉴에서 고를 수 있습니다.

화면 언어는 설정 → 모양에서 **한국어 · English** 중에 고릅니다. AI 가 쓰는 문서(일지·회고·플래너)의 언어는 따로 지정할 수 있고, 지정하지 않으면 화면 언어를 따라갑니다.

## 지원 에이전트

`AGENTS.md` 를 읽을 수 있는 에이전트라면 무엇이든 동작합니다.

- 별도 설정 없이: **Claude Code · Codex CLI · Gemini CLI · Antigravity · pi**
- 설정 → Agents 에서 규칙 파일을 켜면: **Cursor · Windsurf · GitHub Copilot · aider · Cline · Zed**
- **Claude Code · Claude Desktop** 은 여기서 한 단계 더 — 훅(정확한 세션 감지)과 MCP 도구(구조화 기록·플랜 질의)로 직접 연동됩니다 (v2.2.0). Claude Code 는 **Claude Code 화면**에서 앱 안 에이전트로 직접 구동까지 됩니다 (v2.10.0, Agent Client Protocol)

git 백필 시에는 커밋 서명으로 에이전트를 구분합니다.

## Claude Code 플러그인 — 앱 없이도 시작

터미널 Claude Code 에서 두 줄이면 기록이 시작됩니다:

```
/plugin marketplace add bunhine0452/Ocul-PM
/plugin install oculpm@oculpm
```

플러그인 하나로 전 프로젝트에 구성되는 것:

- **훅 브리지** — 세션 시작·종료가 실시간 신호로 기록됩니다 (로컬 파일 append 한 줄, 네트워크 없음).
- **MCP 도구 7종** — `journal_search` · `journal_read` · `journal_write` · `plan_status` · `plan_update` · `plan_create` · `project_init`(사용자 확인 시 새 프로젝트 추적 시작). 에이전트가 마크다운 규격을 흉내 내는 대신 구조화 도구로 기록해 frontmatter 오류가 원천 차단되고, 쌓인 수백 건을 **작업 시작 전에 되찾습니다** — 그 파일을 예전에 왜 건드렸는지가 검색 한 번으로 나옵니다.
- **스킬 5종 + `/oculpm:standup`** — 기록 규격 · project-inception(설계 시드) · self-audit · run-evals · tdd-workflow.
- `.oculpm` 이 있는 추적 프로젝트에서만 동작하고, 비추적 저장소에는 아무 파일도 만들지 않습니다 — [무엇을 읽고 쓰는지 전체 계약](docs/claude-integration/06-plugin-contract.md).
- 앱 설정의 프로젝트별 훅·MCP 등록과는 **택일**입니다 (동시에 켜면 설정 화면이 경고합니다).

> 앱 안의 **Claude Code 화면**은 이 플러그인 없이도 기록합니다 — 앱이 세션마다 기록 도구(MCP)를 직접 물려 주기 때문입니다. 앱 안 ACP 세션에서는 `/plugin`·`/mcp` 같은 CLI 대화형 명령이 동작하지 않으므로, 플러그인 설치는 터미널에서 합니다. 이 구분은 [위키의 Claude Code 연동](https://oculpm.com/wiki/claude-code) 문서에 정리돼 있습니다.

## 설치

[최신 릴리스](https://github.com/bunhine0452/Ocul-PM/releases/latest)에서 `Ocul-PM_x.y.z_aarch64.dmg` 를 받아 `Applications` 로 드래그하면 끝입니다. macOS(Apple Silicon)용이고, 한 번 설치하면 이후 버전은 앱 안에서 자동으로 업데이트됩니다.

아직 Apple 공증 전이라 처음 열 때 "손상되었기 때문에 열 수 없습니다"가 뜰 수 있습니다. 실제 손상이 아니라 macOS 의 격리 표시 때문이니, 터미널에서 한 줄이면 됩니다:

```bash
xattr -dr com.apple.quarantine /Applications/Ocul-PM.app
```

첫 의미 검색 때 임베딩 모델(약 135MB)을 한 번 내려받습니다. 이후에는 오프라인으로 동작합니다.

앱 설치 없이 기록만 먼저 시작하려면 위의 **Claude Code 플러그인**으로도 됩니다 — 일지·플랜이 `.oculpm/` 마크다운으로 쌓이고, 앱은 나중에 설치해도 그 기록을 그대로 읽습니다.

막히는 게 있으면 [위키](https://oculpm.com/wiki)에 흔한 문제와 해법을 모아 두었습니다.

## 데이터는 어디에 있나

```
your-project/
├── AGENTS.md          # 에이전트가 읽는 기록 규칙 (앱이 심고 버전 관리)
└── .oculpm/
    ├── journal/       # 작업 일지 — 원본(SSOT)
    ├── planner/       # 계획 문서
    ├── discussion/    # 토의 문서
    └── index/         # 앱이 관리하는 캐시 · diff 보관
```

SQLite 는 화면을 빨리 그리기 위한 파생 캐시일 뿐이라 언제든 파일에서 다시 만들 수 있습니다. 일지와 diff 에 실수로 섞여 들어간 API 키·토큰은 저장 전에 자동으로 가려집니다(`[REDACTED]`).

## 기술

Tauri 2 네이티브 앱입니다. Electron 이 아니라서 dmg 가 60MB 를 넘지 않고 콜드 스타트가 1.5초 안에 끝납니다. 백엔드는 Rust(tokio · rusqlite · sqlite-vec), 프론트는 React 19 + TypeScript. 코드 분석은 tree-sitter(Rust · TS · JS · Python · Go), 임베딩은 fastembed 로 전부 로컬에서 돌고, API 키는 DB 가 아니라 OS 키체인에 저장합니다.

## 소스에서 빌드

```bash
git clone https://github.com/bunhine0452/Ocul-PM
cd Ocul-PM
pnpm install
pnpm tauri dev      # 개발 실행
pnpm tauri build    # .dmg / .app 번들
```

Node 18+, pnpm, Rust stable 이 필요하고 macOS 는 Xcode Command Line Tools 도 있어야 합니다.

## 로드맵

- [ ] macOS (Intel) · Windows 빌드
- [ ] Apple 공증
- [ ] 팀 동기화 (옵트인)

## 그리고

이 저장소 자체가 Ocul-PM 으로 추적됩니다. `.oculpm/journal/` 을 열면 이 앱을 만드는 동안 에이전트들이 남긴 일지가 그대로 들어 있습니다. 버그와 아이디어는 [이슈](https://github.com/bunhine0452/Ocul-PM/issues)로, 마음에 들면 Star 하나 눌러 주세요.

## 후원

Ocul-PM 은 혼자 만들고 유지합니다. 앱은 앞으로도 무료지만, 만드는 데에는 시간과 비용이 듭니다. 쓰면서 도움이 됐다면 [Ko-fi 에서 커피 한 잔](https://ko-fi.com/beachcombers)을 보내 주셔도 좋습니다 — 일회성도, 매달도 됩니다.

후원해도, 하지 않아도 받는 기능은 똑같습니다. 아래 약속은 그대로입니다.

## 라이선스와 약속

[MIT](LICENSE) © 2026 Kim Hyunbin

**지금 이 저장소에 있는 기능은 영원히 무료·MIT 입니다.** 개인 사용은 회사 안에서든 밖에서든 영구 무료(Free forever for individuals)이고, 유료화는 앞으로 만들 팀 기능(동기화 서버·팀 뷰 — 별도 모듈)에만 적용됩니다. 코어 기능을 유료 모듈로 옮기는 일은 없습니다.

기여는 CLA 없이 [DCO(sign-off)](CONTRIBUTING.md)로 받습니다 — 코어가 영원히 MIT 로 남기 때문에 저작권을 모아둘 이유가 없습니다.
