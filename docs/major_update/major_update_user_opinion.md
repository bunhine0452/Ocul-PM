# 아키텍쳐 변경 고안
- 사유 : 코드가 변경 될 시에 사용자가 수동으로 변화를 감지하고 이를 기록해야하는 불편함과 기존의 프로그램의 목적의 설계상으로도 적합성이 떨어진다고 판단함.

## 변경안 
### 1. 프로젝트를 생성하거나 기존 프로젝트를 처음 불러 올 시 숨겨진 파일 .oculpm 폴더를 생성
- 생성된 폴더는 프로젝트 내의 파일들의 변경이 일어 날 시에 변화를 감지하고 기록하는 용도로 사용하고 앱과 유기적으로 연동되도록 설정 



기능 구현과 버그에 관하여 기록할 수 있는 마크다운 템플릿(예시) 
```
# 기능 구현 예시

[ ] 추가하고자 하는 기능, 완료 후 체크마크 표시

- 난이도: AI가 아래 기준에 따라 superhigh, high, medium, low, verylow 중 하나를 선택합니다.

<난이도 선정 기준>
* **superhigh**: 엮여진 파일들이 매우 많거나, 앱의 핵심 아키텍처를 크게 변경해야 하는 경우
* **high**: 여러 파일에 걸쳐 수정이 필요하거나, 새로운 데이터베이스 마이그레이션이 포함된 경우
* **medium**: 2~3개 내외의 파일 수정으로 완료되며, 기존 아키텍처를 유지하면서 새로운 컴포넌트나 간단한 로직을 추가하는 경우
* **low**: 단일 파일 내의 로직 수정이나, UI 레이아웃 및 스타일의 단순 변경인 경우
* **verylow**: 오타 수정, 텍스트(문구) 변경, 주석 추가 등 애플리케이션의 핵심 로직이나 파일 구조에 전혀 영향을 주지 않는 단순 작업

- 기능 추가 방법: 해당 기능을 구현하기 위한 단계별 접근 방식(백엔드, 프론트엔드 순서 등)

- 수정, 삭제, 생성된 파일들: 영향을 받는 모든 파일의 경로와 이름, 그리고 작업 유형(Create/Update/Delete)을 명시합니다
---

# 버그 수정 예시

[] 버그 수정 후 체크 표시 (필요하다면 사용자가 정의한 버그를 좀 더 구체적으로 정의 해도 괜찮음)

- 발생 원인: 버그가 발생한 원인을 AI가 분석 
- 해결 방법: 어떤 로직을 어떻게 변경하여 버그를 해결했는지, 단계별 구체적인 접근 방식을 작성
- 수정, 삭제, 생성된 파일들: 영향을 받은 모든 파일의 경로와 이름, 그리고 작업 유형(Update/Delete/Create)을 명시

```
- 이런 방식으로 정형화 하여 json으로 관리하거나 마크다운 파일로 관리하도록 함. 여기서 json을 사용할지 markdown을 사용할지는 장단점을 의논해 봐야함. LLM은 출력하는 텍스트 방식이 차이가 있을 수 있음으로 정형화에 힘 써야함. 추가로 

### 작동방식

- 사용자가 사용하는 환경에 따라 프롬포트를 강제로 모델에게 주입 
    - 예시)
        - Antigravity 사용시 .agent/rules/ocul-pm.md 작성 만약 디렉토리가 없다면 생성

    - 이처럼 gemini_cli , cursor, cladue code, Pi-agent 등 사용자가 사용하는 환경에 따라 .ocul_pm 폴더속 파일들이 코드를 수정하며 agent 가 기록하도록 설정하는게 목적
    
### .oculpm 파일 관리 방식 

- 사용자가 앱을 킨다 -> if 어떠한 프로젝트라도 있을 경우 .ouclpm 폴더속에 "20260522" 와 같은 날짜가 있는 폴더 유무 확인 -> 없다면 생성. 
    
    - 생성된 폴더 속에는 오늘 하루동안 바이브 코딩을했던 사실들을 기록할 것인데, 이 기록들은 /Users/kimhyunbin/Desktop/git/ai-pm/docs/2026521 폴더 속에 나름 예시로 정의 해두었음. 참고로 `2055_bugs.md` 의 파일명은 24시각 기준으로 `시간분_목적.md` 와 같이 지었음.

    - 이 트리거는 여러번 작동하면 안되고 여러 엣지케이스들을 고려하여 작동되어야됨. 그리고 oculpm에서 생성하는 폴더와 외부 llm 이 생성하고 수정하게 될 파일들의 구분을 명확히 지어야함.

### 프론트엔드 UI/UX 

메인페이지에서 프로젝트를 클릭하여 들어가면 overview 프로젝트의 메인 페이지고 today가 별도로 있는데 today를 메인으로 변경한다.

- 이제 .oculpm 폴더 속 파일들을 통해 today에 보여준다. 추후 구체적인 편의성과 UI/UX 를 더 논의해봐야함. 


이렇게 생각해봤는데 어때? 먼저 나랑 회의를 하자. 네 답변과 질문들을 docs/major_update/ 경로에 마크다운 파일을 생성해줘 




<질문 답변>
Q1 : yes
Q2 : yes
Q3 : C
Q4 : 1차 지원 에이전트 목록에 동의 
Q5 : yes
Q6 : A
Q7 : yes
Q8 : yes
Q9 : yes
Q10 : (a) 타임라인 + 카테고리 필터
Q11 : yes

<추가 의도 답변>
1. 1차 타깃 사용자는 개인 개발자
2. 외부에 노출은 x 일단은 노출 안하는것으로 결정 
3. 한 건의 작업은 한건의 작업은 세션단위
4. 개인

이 답변을 통해 대략 방향성이 잡혔고 이제 구현계획을 매우 자세하고 구체적으로 작성해줘

