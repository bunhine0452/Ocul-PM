-- 회고 화면 삭제 (2026-09-08) — 그 화면만 읽고 쓰던 캐시 테이블을 떨군다.
--
-- 022 는 등록된 채로 남는다: 번호는 재사용하지 않고, 이미 022 를 돌린 DB 의
-- 이력이기도 하다. 022 가 만든 것을 여기서 되돌리는 것이 순서다.
--
-- SSOT 원칙상 잃는 데이터는 없다 — 회고 본문의 원본은 `.oculpm/retro/*.md`
-- 였고 이 테이블은 파생 캐시였다. 그 파일들도 앱이 더는 읽지 않지만 사용자의
-- 것이라 지우지 않는다 (앱이 사용자 저장소의 마크다운을 지우는 일은 없다).
DROP TABLE IF EXISTS retro_insights;
