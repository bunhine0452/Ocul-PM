# ocul-pm 작업 기록 규칙

@AGENTS.md

> 이 섹션은 ocul-pm 이 관리하는 블록 안에 있습니다 (`<!-- oculpm:begin v1 -->` … `<!-- oculpm:end -->`).
> 실제 규칙은 프로젝트 루트의 `AGENTS.md` 를 따르세요. agent.id 는 `zed` 로 적습니다.
> 일지를 쓴 **직후** `AGENTS.md` §7 대로 대응 **Planner 항목도 갱신**하세요 (`.oculpm/planner/*.md` 의 글리프 + 갱신 로그).
